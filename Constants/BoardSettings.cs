internal static class BoardSettings
{
    /// <summary>
    /// 盤面の一辺の長さ(正方形であることが前提)
    /// </summary>
    internal static readonly int BOARD_SIZE = 3;

    internal static readonly int CELL_COUNT = BOARD_SIZE * BOARD_SIZE;
}